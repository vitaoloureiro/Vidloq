@model Vidloq.ViewModels.QualquerProdutoViewModel

@{
    ViewBag.Title = "Produto Aleatório";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

@{
    // se o filme foi locado por mais de 5 cliente (> 5) então é popular (? "popular") : senão (não faça nada)
    var popular = Model.Clientes.Count > 2 ? "popular" : null;
}

<style>
    .popular {color: red;}
</style>

<h2 class="@popular">@Model.Produto.Titulo</h2>

@if (Model.Clientes.Count == 0)

{
    <p>Este produto nunca foi locado.</p>
}
else
{
    <ul>
        @foreach (var cliente in Model.Clientes)
        {
            <li>@cliente.Nome</li>
        }
    </ul>
}